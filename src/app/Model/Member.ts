export interface Member {
  id: number;
  firstName: string;
  lastName: string;
  identityNumber: string;
  accessDate: Date;
  nationality: string;
  // Add other attributes as per your Member entity
}
